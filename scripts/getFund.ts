import { NonceManager } from '@ethersproject/experimental'
import { ethers } from 'hardhat';
import { privateKey, eoa } from '../data';
// import getProvider from '../uitls/getProvider';

const main = async () => {

	const tx = {
		to: eoa,
		// value: ethers.utils.parseEther("80.0")
	};

}

main()